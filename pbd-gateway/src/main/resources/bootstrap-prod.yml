spring:
  profiles :
    active : prod
  cloud:
    config:
      discovery:
        enabled: true
        service-id: pbd-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/